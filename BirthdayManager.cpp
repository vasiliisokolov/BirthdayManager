#include <iostream>
#include <ctime>
#include <map>

int main()
{
    std::string input = "";
    std::map<std::time_t, std::string> list;
    std::cout << "Birthday Manger." << std::endl;
    while (input != "end")
    {
        std::cout << "Enter the name: ";
        std::cin >> input;
        if (input == "end")
        {
            break;
        }
        else
        {

        }
    }
}

